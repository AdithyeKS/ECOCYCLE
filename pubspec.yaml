name: ecocycle_1
description: EcoCycle – Smart waste & cloth recycling (Supabase + OSM)
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter: { sdk: flutter }
  cupertino_icons: ^1.0.8

  # Auth + DB
  supabase_flutter: ^2.6.0

  # Maps (OpenStreetMap)
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  geolocator: ^10.1.0

  # UI helpers
  flutter_animate: ^4.5.0
  intl: ^0.19.0
  image_picker: ^1.2.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/ecocycle.png

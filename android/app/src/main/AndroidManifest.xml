<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="${applicationId}">

    <!-- Permissions -->
    <!-- Internet required to fetch map tiles and talk to Supabase -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Location permissions (request at runtime in Flutter using geolocator) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Optional: background location if you need it (requires special handling on Android 11+) -->
    <!-- <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" /> -->

    <application
        android:label="ecocycle_1"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- Flutter normal theme used while Flutter initializes (do not delete) -->
        <meta-data
            android:name="io.flutter.embedding.android.NormalTheme"
            android:resource="@style/NormalTheme"
            />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            If you ever decide to use Google Maps instead of OSM, add your API key here:
            Replace YOUR_GOOGLE_MAPS_API_KEY with your key.
            For OpenStreetMap (flutter_map) this is not required.
        -->
        <!--
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_GOOGLE_MAPS_API_KEY_HERE" />
        -->

        <!-- Don't delete this meta-data:
             The Flutter tool uses it to generate GeneratedPluginRegistrant.java -->
    </application>

    <!-- Required to query activities that can process text (used by some plugins) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
